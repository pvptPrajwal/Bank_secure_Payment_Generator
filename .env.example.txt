# Copy this file to ".env" (same folder as main.py / the .exe file).
# Use the PUBLISHABLE key, NOT the secret key.
#
# Supabase Dashboard → Project Settings → API Keys
#   → "Publishable and secret API keys" tab
#   → copy the "Publishable key" (starts with sb_publishable_...)
#     and the "Project URL"

SUPABASE_URL=
SUPABASE_KEY=